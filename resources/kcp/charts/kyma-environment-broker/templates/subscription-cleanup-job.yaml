apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: { .Chart.Name }}-subscription-cleanup-job
spec:
  jobTemplate:
    metadata:
      name: { .Chart.Name }}-subscription-cleanup-job
    spec:
      template:
        metadata:
          annotations:
            sidecar.istio.io/inject: “false”
        spec:
          containers:
            - image: agalwas/subscription-cleanup-job:latest
              name: { .Chart.Name }}-subscription-cleanup-job
              env:
                - name: APP_GARDENER_PROJECT
                  value: {{ .Values.gardener.project }}
                - name: APP_GARDENER_KUBECONFIG_PATH
                  value: {{ .Values.gardener.kubeconfigPath }}
              volumeMounts:
                - mountPath: /gardener/kubeconfig
                  name: gardener-kubeconfig
                  readOnly: true
              resources: {}
          restartPolicy: OnFailure
          volumes:
            - name: gardener-kubeconfig
              secret:
                secretName: {{ .Values.gardener.secretName }}
  schedule: '*/10 * * * *'
status: {}
# Default values for component-reconcilers.

nameOverride: ""
fullnameOverride: ""

deployment:
  imagePullPolicy: "IfNotPresent"
  nodeSelector: {}

service:
  type: ClusterIP
  port: 8080
  istioPort: 15020

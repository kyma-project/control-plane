{{ $global := . }}
{{- range $component := .Values.global.images.components }}
{{ if not (hasKey $global.Subcharts $component.name) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $component.name }}-reconciler
  namespace: {{ $global.Release.Namespace }}
  labels:
    kyma-project.io/component-reconciler: ""
    component: {{ $component.name }}
spec:
  type: {{ $global.Values.service.type }}
  ports:
    - port: {{ $global.Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ $global.Values.service.istioPort }}
      targetPort: {{ $global.Values.service.istioPort }}
      protocol: TCP
      name: istio-http
  selector:
    kyma-project.io/component-reconciler: ""
    component: {{ $component.name }}
---
{{- end }}
{{- end }}
